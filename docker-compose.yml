services:
  endfield-auto:
    build: .
    container_name: endfield-auto
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Asia/Shanghai}
    volumes:
      - ./.data:/app/.data
